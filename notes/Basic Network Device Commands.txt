show mac-address-table
- all switches maintain a MAC address table

show route
- routes maintain a list of next hops (routing table)

show interface
- status of interface (up, down, connected, disabled, speed, duplex)
- MTU, encapsulation type
- CRC errors, drops

show config
- view device settings

show arp

show vlan

show power
- power supply status, power over ethernet usage
